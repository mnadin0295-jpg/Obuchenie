word = 'test'  # Можно заменить на любое слово

length = len(word)
middle = length // 2

if length % 2 == 0:
    # Чётное количество букв — выводим две средние буквы
    print(word[middle - 1: middle + 1])
else:
    # Нечётное количество букв — выводим одну среднюю букву
    print(word[middle])
